# BUG-0039: Withdrawal does not clear approved_at and po_number fields

> **Plan Status:** Reviewed & Approved
> **Created:** 2026-04-11
> **Reviewed By:** AI Code Review Agent
> **Severity:** minor

## 1. Bug Details

| Field | Value |
|---|---|
| **Ticket** | BUG-0039 |
| **Title** | Withdrawal does not clear approved_at and po_number fields |
| **Description** | When an RFPO is withdrawn from approval via withdraw_approval(), the status is reset to 'Draft' but the approved_at timestamp and po_number fields are not cleared. If the RFPO was previously approved in a different cycle, these stale values persist, causing incorrect data in reports and the explorer UI (showing an approved date and PO number for a Draft RFPO). |
| **Submitted By** | System Administrator |
| **Priority** | medium |
| **Severity** | minor |
| **Status** | Open |
| **Created** | 2026-04-12 |
| **Page Context** | /api/rfpos/<id>/withdraw-approval |
| **Steps to Reproduce** | 1. Submit and approve an RFPO (it gets approved_at date and po_number). 2. Admin withdraws the RFPO. 3. RFPO status is Draft but approved_at and po_number are still set. 4. Reports show this RFPO as having been approved on a date, despite being Draft. |

## 2. Root Cause Analysis

The bug is a **missing field reset** in the `withdraw_approval()` route handler in `simple_api.py` (line ~2069).

When an RFPO is approved, the approval flow in the `take_approval_action()` route (line ~1331) sets:
- `instance.rfpo.approved_at = datetime.utcnow()`
- `instance.rfpo.po_number = RFPO.generate_po_number(abbrev)`

When an admin later withdraws the RFPO via `withdraw_approval()` (line ~2069), the code:
1. Deletes the `RFPOApprovalInstance` (cascading to actions) ✓
2. Resets `rfpo.status = "Draft"` ✓
3. Updates `rfpo.updated_by` and `rfpo.updated_at` ✓
4. **Does NOT null out `rfpo.approved_at`** ✗
5. **Does NOT null out `rfpo.po_number`** ✗

This is a simple omission — two lines were never added to the withdrawal logic. The fields retain stale values from the previous approval cycle, creating a data inconsistency where a Draft RFPO appears to have been approved.

The `po_number` column has a `unique=True` constraint, which means the stale PO number also blocks the same PO sequence number from being reused if it should be, though in practice `generate_po_number()` increments a sequence so this is unlikely to cause a collision.

## 3. Impact Assessment

**Affected files/functions:**
- `simple_api.py`: `withdraw_approval()` route (~line 2069)

**User-facing impact:**
- RFPO Explorer shows an "Approved" date and PO number for Draft RFPOs
- Reports that filter or sort by `approved_at` include withdrawn RFPOs incorrectly
- PDF regeneration may include stale PO numbers

**Data impact:**
- No data corruption — the fields simply aren't cleared when they should be
- Stale `po_number` values are semantically wrong but don't violate referential integrity
- The `approved_at` index (`idx_rfpo_status_approved`) returns misleading results

**Scope:**
- Any RFPO that was approved and then withdrawn is affected
- Low frequency scenario but high confusion when it occurs

## 4. Fix Strategy

### Phase 1: Core Fix (2 lines)
Add two field resets in `withdraw_approval()` immediately after `rfpo.status = "Draft"`:

```python
# Reset RFPO status to Draft
rfpo.status = "Draft"
rfpo.approved_at = None      # ← ADD: clear stale approval timestamp
rfpo.po_number = None         # ← ADD: clear stale PO number
rfpo.updated_by = user.get_display_name()
rfpo.updated_at = datetime.utcnow()
```

**Rationale:** These two fields are set during approval (lines 1331, 1334-1339) and must be symmetrically cleared during withdrawal to maintain data consistency. Setting them to `None` is safe because:
- `approved_at` is `nullable=True`
- `po_number` is `nullable=True`

### Phase 2: Hardening — Audit trail enrichment
Include the cleared values in the audit log so there's a record of what was removed:

```python
audit.set_details({
    "rfpo_id": rfpo.rfpo_id,
    "instance_id": instance.instance_id,
    "reason": request.get_json().get("reason", "") if request.is_json else "",
    "cleared_approved_at": str(original_approved_at),   # ← ADD
    "cleared_po_number": original_po_number,             # ← ADD
})
```

Capture the original values before clearing them so the audit trail preserves the history.

### Phase 3: Test coverage
Add an integration test to `tests/integration/test_api_approvals.py` in the `TestWithdrawApproval` class that verifies `approved_at` and `po_number` are cleared after withdrawal.

## 5. Database Impact

No database changes required. The `approved_at` and `po_number` columns already exist and are nullable. The fix simply sets them to `None` during withdrawal.

**Note:** Existing RFPOs that were previously withdrawn but still have stale `approved_at`/`po_number` values are NOT automatically fixed by this code change. A one-time data cleanup query could be run if needed:

```sql
-- Optional: clean up existing stale data
UPDATE rfpos SET approved_at = NULL, po_number = NULL
WHERE status = 'Draft' AND (approved_at IS NOT NULL OR po_number IS NOT NULL);
```

## 6. Detailed Task Breakdown

| # | Task | File(s) | Effort | Phase |
|---|------|---------|--------|-------|
| 1 | Capture original `approved_at` and `po_number` before clearing | `simple_api.py` | Small | 2 |
| 2 | Add `rfpo.approved_at = None` after status reset | `simple_api.py` | Small | 1 |
| 3 | Add `rfpo.po_number = None` after status reset | `simple_api.py` | Small | 1 |
| 4 | Add cleared values to audit log details | `simple_api.py` | Small | 2 |
| 5 | Add integration test for field clearing on withdrawal | `tests/integration/test_api_approvals.py` | Medium | 3 |
| 6 | (Optional) Run data cleanup for existing stale records | SQL migration | Small | 2 |

## 7. Testing Strategy

**Regression tests:**
- Test that withdrawal sets `approved_at` to `None`
- Test that withdrawal sets `po_number` to `None`
- Test that withdrawal still sets status to `"Draft"` (existing behavior)
- Test that the audit log includes the cleared values

**Edge case tests:**
- Withdraw an RFPO that was never approved (both fields already `None`) — should succeed without error
- Withdraw and re-submit/re-approve — verify new `approved_at` and `po_number` are generated fresh

**Manual verification:**
1. Approve an RFPO via the admin panel
2. Verify `approved_at` and `po_number` are set in the database
3. Withdraw the RFPO
4. Verify both fields are `NULL` in the database
5. Check RFPO Explorer — no approved date or PO number shown for the Draft RFPO

**Existing test coverage:** The `TestWithdrawApproval` class in `test_api_approvals.py` currently only tests that non-admins are rejected. There are zero tests for successful withdrawal behavior, making this a test gap.

## 8. Regression Prevention

- Add the integration test from Phase 3 to prevent this from recurring
- The test should assert on all three fields: `status == "Draft"`, `approved_at is None`, `po_number is None`
- Consider adding a model-level method `rfpo.reset_to_draft()` that encapsulates all the field resets, so future developers don't need to remember which fields to clear

## 9. Security Considerations

No security implications. The fix only clears nullable fields during an already-authorized admin operation. The existing permission check (`RFPO_ADMIN` or `GOD`) is sufficient.

## 10. Risk Assessment

| Risk | Severity | Mitigation |
|------|----------|------------|
| Clearing `po_number` on a PO that has been sent to a vendor | Low | Withdrawal is only allowed for "Pending Approval" status, not "Approved" RFPOs that may have been acted upon |
| Unique constraint violation if PO number is reused | Very Low | `generate_po_number()` uses monotonic sequence; freed numbers are not reused |
| Breaking existing reports that rely on `approved_at` being set | Very Low | Reports should already filter by status; clearing stale data improves accuracy |

## 11. Review Summary

| Finding | Severity | Resolution |
|---------|----------|------------|
| Core fix is correct — two nullable fields need to be set to None | — | Confirmed: both fields are nullable, setting to None is safe |
| Audit trail should capture cleared values | Should Fix | Added to Phase 2 — capture originals before clearing |
| Test gap for withdrawal success path | Must Fix | Added integration test in Phase 3 |
| Existing stale data not auto-fixed | Informational | Optional SQL cleanup documented; not critical since withdrawal frequency is low |

**Verdict:** Approve — straightforward 2-line fix with audit trail enrichment and test coverage.

## 12. Files Changed Summary

| File | Change Type | Phase |
|------|-------------|-------|
| `simple_api.py` | Modified — add field resets + audit enrichment in `withdraw_approval()` | 1, 2 |
| `tests/integration/test_api_approvals.py` | Modified — add withdrawal success test | 3 |